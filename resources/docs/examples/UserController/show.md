# Get user details

Returns the full profile information for a specific user.

## Includes

The response includes the user's profile, roles, and permissions.
Sensitive fields (like email) are only visible to the user themselves or admins.
